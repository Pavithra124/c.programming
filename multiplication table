//printing multiplication table of a number
#include<stdio.h>
int main(){
	int a,b,c;
	printf("enter the num of multiplication table");
	scanf("%d",&a);
	printf("the multiplication table of %d is\n",a);
	for(b=1;b<=10;++b)
	{
		printf("%d*%d=%d\n",a,b,a*b);
	}
}
